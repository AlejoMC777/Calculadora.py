import pytest
from Operaciones import area_cuadrado,area_circulo,area_triangulo,area_trapecio,area_hexagono,area_trapecio_isosceles,area_triangulo_equilatero,area_rombo
import math



#Funciones
def area_cuadrado():
    assert area_cuadrado() == 
    assert area_cuadrado() ==
    
def area_circulo():
    assert area_circulo() ==
    assert area_circulo() ==

def area_triangulo():
    assert area_triangulo() ==
    assert area_triangulo() ==
    
def area_trapecio():
    assert area_trapecio() ==
    assert area_trapecio() ==
    
def area_hexagono():
    assert area_hexagono() ==
    assert area_hexagono() ==
    
def area_trapecio_isosceles():
    assert area_trapecio_isosceles() ==
    assert area_trapecio_isosceles() ==  
    
def area_triangulo_equilatero():
    assert area_triangulo_equilatero() ==
    assert area_triangulo_equilatero() ==
    
def area_rombo():
    assert area_rombo() ==
    assert area_rombo() ==